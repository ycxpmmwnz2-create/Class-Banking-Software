# Emulator-only Functions parameter values for the Phase 2B Item 8 gate-ON
# acceptance project. See the gate-off sibling file for why a dotenv file is
# the only non-interactive way to supply declared parameter values to the
# Firebase CLI.
#
# This file is loaded only when the CLI is invoked with
# --project demo-morgan-bank-phase2b-server-test, which is a Firebase demo
# project. It can never apply to the production `morgan-bank` project.
MULTI_TEACHER_V2_ENABLED=true
MULTI_TEACHER_V2_RELEASE_ID=staging-support-functions-v1
MORGAN_BANK_DEPLOYMENT_TIER=production
MORGAN_BANK_STAGING_PROJECT_ID=

# Unrelated Version 3 provider behavior stays disabled in Phase 2B tests. The
# fake key satisfies non-interactive Functions discovery and is never used.
VERSION3_GEMINI_ENABLED=false
VERSION3_GEMINI_RELEASE_ID=disabled-phase2b-emulator
GEMINI_API_KEY=test-only-unused-demo-key
